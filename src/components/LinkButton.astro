---
const { href } = Astro.props;
---

<a 
    href={href}
    class="inline-flex items-center px-4 py-2 text-sm font-medium text-gray-900 hover:text-blue-500 bg-gray-100 border border-blue-500 rounded-full hover:scale-110 hover:bg-gray/10 transition"
>
    <slot/>
</a> 